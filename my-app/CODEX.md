# Codex Instructions

Use `AGENTS.md` as the primary project instruction file.

Before implementing larger features, check `docs/specs/` for the current product, UI, and data model assumptions. If those files are incomplete, make the smallest reasonable implementation and note what needs a product decision.
