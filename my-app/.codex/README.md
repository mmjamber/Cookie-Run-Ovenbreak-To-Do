# Codex Workspace Notes

This directory holds local Codex-facing notes for the project. The canonical shared agent instructions are in `../AGENTS.md`.

Recommended task flow:

1. Read `../AGENTS.md`.
2. Check `../docs/specs/README.md`.
3. Update specs when the requested behavior changes product, UI, or data assumptions.
4. Run `npm run lint` after code changes when feasible.
